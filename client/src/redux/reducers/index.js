import { combineReducers } from 'redux';
import * as type from '../types';

const chatStates = {
    chatList: [],
    socketId: null
}

const chatReducer = (state = chatStates, action){
    // switch(action.type){
    //     case type.
    // }
}